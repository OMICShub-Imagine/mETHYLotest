% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mETHYLotest.EPIC.QC.R
\name{mETHYLotest.EPIC.QC}
\alias{mETHYLotest.EPIC.QC}
\title{EPIC Array QC Pipeline}
\usage{
mETHYLotest.EPIC.QC(
  myLoad,
  beta_matrix = NULL,
  outputDir = NULL,
  pheno_columns = NULL,
  sample_colors = NULL,
  doPCA = TRUE,
  doTSNE = TRUE,
  doSVD = TRUE,
  doBetaDensity = TRUE,
  doCorHeatmap = TRUE,
  doDetPSummary = TRUE,
  det_p_cut = 0.01,
  doSexPrediction = TRUE,
  run_interactive_ui = TRUE
)
}
\arguments{
\item{myLoad}{ChAMP object with \code{$beta} and \code{$pd}.}

\item{beta_matrix}{Optional replacement beta matrix — overrides
\code{myLoad$beta} if provided.}

\item{outputDir}{Output directory for Excel and HTML report.
Defaults to \code{tempdir()/mETHYLotest_QC} if \code{NULL}.}

\item{pheno_columns}{Columns of \code{$pd} used for SVD correlation.
Auto-detected from extra columns if \code{NULL}.}

\item{sample_colors}{Named colour vector per group label
(e.g. \code{c("CTL" = "steelblue", "CASE" = "firebrick")}).}

\item{doPCA}{Logical. Compute PCA? (default \code{TRUE})}

\item{doTSNE}{Logical. Compute t-SNE? (default \code{TRUE})}

\item{doSVD}{Logical. Compute SVD heatmap? (default \code{TRUE})}

\item{doBetaDensity}{Logical. Plot beta distribution? (default \code{TRUE})}

\item{doCorHeatmap}{Logical. Plot sample correlation heatmap?
(default \code{TRUE})}

\item{doDetPSummary}{Logical. Plot detection p-value summary?
Requires \code{myLoad$detP}. (default \code{TRUE})}

\item{det_p_cut}{Numeric. Detection p-value threshold used for
the summary plot. (default \code{0.01})}

\item{doSexPrediction}{Logical. Predict sex from chromosomal methylation?
(default \code{TRUE})}

\item{run_interactive_ui}{Logical. Launch interactive UI? (default \code{TRUE})}
}
\value{
Invisibly returns a character vector of sample names flagged for
  removal. Returns \code{character(0)} if no samples are flagged or the UI
  is skipped.
}
\description{
Runs the full QC pipeline on a ChAMP \code{myLoad} object:
\enumerate{
  \item PCA (\code{stats::prcomp})
  \item t-SNE (\code{Rtsne})
  \item SVD covariate correlation (\code{PCAtools::pca})
  \item Beta value density distribution
  \item Sample correlation heatmap
  \item Detection p-value summary
  \item Sex prediction from chromosomal methylation
  \item Excel export of all QC metrics
  \item HTML report generation from Rmd template
  \item Interactive sample-removal UI (\code{mETHYLotest.EPIC.QC.UI})
}
}
\seealso{
\code{\link{mETHYLotest.EPIC.QC.UI}},
  \code{\link{mETHYLotest.EPIC.utils.SVDCorrelation}},
  \code{\link{mETHYLotest.EPIC.utils.PlotPCATSNE}},
  \code{\link{mETHYLotest.EPIC.utils.PlotBetaDensity}},
  \code{\link{mETHYLotest.EPIC.utils.PlotCorHeatmap}},
  \code{\link{mETHYLotest.EPIC.utils.PlotDetPSummary}},
  \code{\link{mETHYLotest.EPIC.utils.PlotSexPrediction}}
}
