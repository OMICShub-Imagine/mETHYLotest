% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mETHYLotest.EPIC.utils.R
\name{mETHYLotest.EPIC.utils.PlotCorHeatmap}
\alias{mETHYLotest.EPIC.utils.PlotCorHeatmap}
\title{Sample Correlation Heatmap (interactive)}
\usage{
mETHYLotest.EPIC.utils.PlotCorHeatmap(myLoad, method = "pearson")
}
\arguments{
\item{myLoad}{ChAMP object with \code{$beta} and \code{$pd}.}

\item{method}{\code{"pearson"} (default) or \code{"spearman"}.}
}
\value{
A \code{plotly} object with an \code{"outlier_samples"} attribute,
  or \code{NULL}.
}
\description{
Computes the Pearson (or Spearman) inter-sample correlation matrix and
renders it as an interactive \code{plotly} heatmap with hierarchical
clustering order. Outlier samples are flagged via an attribute.

Uses \code{plotly::plot_ly(type = "heatmap")} directly to avoid
\code{ggplotly} bridging issues with \code{geom_tile} + \code{geom_text}.
}
