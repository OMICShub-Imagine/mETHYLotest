% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mETHYLotest.EPIC.utils.R
\name{mETHYLotest.EPIC.utils.PlotBetaDensity}
\alias{mETHYLotest.EPIC.utils.PlotBetaDensity}
\title{Beta Value Distribution Plot}
\usage{
mETHYLotest.EPIC.utils.PlotBetaDensity(
  myLoad,
  sample_colors = NULL,
  n_probes_sample = NULL,
  seed = 42L
)
}
\arguments{
\item{myLoad}{ChAMP object with \code{$beta} and \code{$pd}.}

\item{sample_colors}{Named colour vector per group level.}

\item{n_probes_sample}{Integer, \code{NULL} (adaptive), or \code{Inf}
(all probes). See description.}

\item{seed}{Integer. Random seed for reproducible subsampling
(default 42).}
}
\value{
A \code{plotly} object or \code{NULL}.
}
\description{
Per-sample density plot of beta values using native plotly traces.
The number of probes used for density estimation adapts automatically
to the array size and sample count:
\itemize{
  \item If \code{n_probes_sample = NULL} (default): uses all probes
    when n_samples <= 24, otherwise subsamples adaptively.
  \item Explicit integer value: forces that subsample size.
  \item \code{Inf}: always uses all probes.
}
}
